filter = {
    show_vanilla = yes
    trigger = {
        NAND = {
            file = common/national_focus/paraguay_uruguay_shared_branch.txt
            key = choice
        }

	NOT = {
            # should be trait_engineer
            text = "unit leader trait engineer not defined"
            # should be trait_reckless
            text = "unit leader trait reckless not defined"
            # not in vanilla
            text = "country leader trait ayatollah_trait not defined"
	}

	NOT = {
	    # ok to ignore
	    text = "sprite is redefined by an identical sprite"
	}

	NOT = {
            key = suggest-localization
	}
    }
}
